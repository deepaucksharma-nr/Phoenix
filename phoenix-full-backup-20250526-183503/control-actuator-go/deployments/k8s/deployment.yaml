apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-actuator-go
  labels:
    app.kubernetes.io/name: control-actuator-go
    app.kubernetes.io/part-of: phoenix-platform
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: control-actuator-go
  template:
    metadata:
      labels:
        app.kubernetes.io/name: control-actuator-go
    spec:
      containers:
      - name: control-actuator-go
        image: ghcr.io/phoenix/control-actuator-go:latest
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ENVIRONMENT
          value: production
        livenessProbe:
          httpGet:
            path: /health
            port: http
        readinessProbe:
          httpGet:
            path: /ready
            port: http
