apiVersion: apps/v1
kind: Deployment
metadata:
  name: benchmark
  labels:
    app.kubernetes.io/name: benchmark
    app.kubernetes.io/part-of: phoenix-platform
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: benchmark
  template:
    metadata:
      labels:
        app.kubernetes.io/name: benchmark
    spec:
      containers:
      - name: benchmark
        image: ghcr.io/phoenix/benchmark:latest
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ENVIRONMENT
          value: production
        livenessProbe:
          httpGet:
            path: /health
            port: http
        readinessProbe:
          httpGet:
            path: /ready
            port: http
